# Deployment Guide

## Vercel Deployment

The application is optimized for Vercel deployment:
1. Connect your GitHub repository to Vercel
2. Configure build settings (default Next.js settings apply)
3. Deploy!

For environment variables, configure them via the Vercel dashboard.
